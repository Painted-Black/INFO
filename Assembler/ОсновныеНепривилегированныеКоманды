-----------------------------------------------------------------------------------------------------
| mov приемник, источник                | пересылка данных                                          |
| cmovсс приемник, источник             | условная пересылка данных                                 |
| xchg операнд1, операнд2               | обмен операндов между собой                               |
| bswap регистр32                       | обмен байтоввнутри регистра                               |
| push источник                         | поместить данные в стек                                   |
| pop приемник                          | считать данные из стека                                   |
| pusha                                 | поместить в стек все регистры общего назначения           |
| pushad                                | поместить в стек все регистры общего назначения           |
| popa                                  | загрузить из стека все регистры общего назначения         |
| popad                                 | загрузить из стека все регистры общего назначения         |
| in приемник, источник                 | считать данные из порта                                   |
| out приемник, источник                | записать данные в порт                                    |
| cwd                                   | конвертирование слова в двойное слово                     |
| cdq                                   | конвертирование двойного слова в учетверенное             |
| cbw                                   | конвертирование байта в слово                             |
| cwde                                  | конвертирование слова в двойное слово                     |
| movsx приемник, источник              | пересылка с расширением знака                             |
| movzx приемник, источник              | пересылка с расширением нулями                            |
| xlat адрес                            | трансляция в соответствии с таблицей                      |
| xlatb адрес                           | трансляция в соответствии с таблицей                      |
| lea приемник, источник                | вычисление эффективного адреса                            |
-----------------------------------------------------------------------------------------------------
| add приемник, источник                | сложение, приемник = приемник + источник                  |
| adc приемник, источник                | сложение с переносом, приемник = приемник + источник + CF |
| xadd приемник, источник               | обменять между собой и сложить                            |
| sub приемник, источник                | вычитание, приемник = приемник - источник                 |
| sbb приемник, источник                | вычитание с займом, применик = приемник - источник - CE   |
| imul источник                         | умножение чисел со знаком, ax = ax * источник             |
| imul приемник, источник               | умножение чисел со знаком, приемник *= источник           |
| imul приемник, источник1, источник2   | умножение чисел со знаком, приемник=источник1*источник2   |
| mul источник                          | умножение чисел без знака, ax = ax * источник             |
| idiv источник                         | целочисленное деление со знаком, ax = ax / источник       |
| div источник                          | целочисленное деление без знака, ax = ax / источник       |
| inc приемник                          | инкремент                                                 |
| dec приемник                          | декремент                                                 |
| neg приемник                          | изменение знака (операция дополнения до двух)             |
| cmp приемник, источник                | сравнение                                                 |
| cmpxchg приемник, источник            | сравнить и обменять между собой                           |
| cmpxchg8b приемник                    | сравнить и обменять 8 байт                                |
-----------------------------------------------------------------------------------------------------
| daa                                   | BCD-коррекция после сложения                              |
| das                                   | BCD-коррекция после вычитания                             |
| aaa                                   | ASCII-коррекция после сложения                            |
| aas                                   | ASCII-коррекция после вычитания                           |
| aam                                   | ASCII-коррекция после умножения                           |
| aad                                   | ASCII-коррекция перед делением                            |
-----------------------------------------------------------------------------------------------------
| and приемник, источник                | логическое И, приемник = приемник & источник              |
| or приемник, источник                 | логическое ИЛИ, приемник = приемник | источник            |
| xor приемник, источник                | логическое исключающее ИЛИ, приемник=приемник XOR источник|
| not приемник                          | инверсия                                                  |
| test приемник, источник               | логическое сравнение                                      |
-----------------------------------------------------------------------------------------------------
| sar приемник, источник                | арифметический сдвиг вправо                               |
| sal приемник, источник                | арифметический сдвиг влево                                |
| shr приемник, источник                | логический сдвиг вправо                                   |
| shl приемник, источник                | логический сдвиг влево                                    |
| shrd приемник, источник, счетчик      | сдвиг повышенной точности вправо                          |
| shld приемник, источник, счетчик      | сдвиг повышенной точности влево                           |
| ror приемник, счетчик                 | циклический сдвиг вправо                                  |
| rol приемник, счетчик                 | циклический сдвиг влево                                   |
| rcr приемник, счетчик                 | циклический сдвиг вправо через флаг переноса              |
| rcl приемник, счетчик                 | циклический сдвиг влево через флаг переноса               |
-----------------------------------------------------------------------------------------------------
| bt база, смещение                     | проверка бита                                             |
| bts база, смещение                    | проверка и установка бита                                 |
| btr база, смещение                    | проверка и сброс бита                                     |
| btc база, смещение                    | проверка и инверсия бита                                  |
| bsd приемник, источник                | прямой поиск бита                                         |
| bsr приемник, источник                | обратный поиск бита                                       |
| setcc приемник                        | установка байта по условию                                |
-----------------------------------------------------------------------------------------------------
| jmp операнд                           | безусловный переход                                       |
| jcc метка                             | условный переход                                          |
| jcxz метка                            | переход, если CX == 0                                     |
| jecxz метка                           | переход, если ECX == 0                                    |
| loop метка                            | цикл, CX - счетчик, не изменяет значение флагов           |
| loope метка                           | цикл, пока равно, ZF == 1 ?                               |
| loopz метка                           | цикл, пока ноль, ZF == 1 ?                                |
| loopne метка                          | цикл, пока не равно, ZF == 0 ?                            |
| loopnz метка                          | цикл, пока не ноль, ZF == 0 ?                             |
| call операнд                          | вызов процедуры                                           |
| ret число                             | возврат из процедуры                                      |
| retn число                            | возврат из процедуры                                      |
| retf число                            | возврат из процедуры                                      |
| int число                             | вызов прерывания                                          |
| iret                                  | возврат из обработчика прерывания                         |
| iretd                                 | возврат из обработчика прерывания                         |
| int3                                  | вызов прерывания 3                                        |
| int4                                  | вызов прерывания 4 при переполнении                       |
| bound индекс, границы                 | проверка выхода за границы массива                        |
| enter размер, уровень                 | вход в процедуру                                          |
| leave                                 | выход из процедуры                                        |
-----------------------------------------------------------------------------------------------------
| rep                                   | повторять                                                 |
| repe                                  | повторять, пока равно (пока ZF == 1)                      |
| repne                                 | повторять, пока не равно (пока ZF == 0)                   |
| repz                                  | повторять, пока ноль (пока ZF == 1)                       |
| repnz                                 | повторять, пока не ноль (пока ZF == 0)                    |
| movs премник, источник                | копирование строки (из DS:SI в ES:DI)                     |
| movsb                                 | копирование строки байтов (из DS:SI в ES:DI)              |
| movsw                                 | копирование строки слов (из DS:SI в ES:DI)                |
| movsd                                 | копирование строки двойных слов (из DS:SI в ES:DI)        |
| cmps премник, источник                | сравнение строк (из DS:SI с ES:DI)                        |
| cmpsb                                 | сравнение строк байтов (из DS:SI с ES:DI)                 |
| cmpsw                                 | сравнение строк слов (из DS:SI с ES:DI)                   |
| cmpsd                                 | сравнение строк двойных слов (из DS:SI с ES:DI)           |
| scas приемник                         | сканирвование строки (сравнивает AX с ES:DI)              |
| scasb                                 | сканирвование строки байтов (сравнивает AX с ES:DI)       |
| scasw                                 | сканирвование строки слов (сравнивает AX с ES:DI)         |
| scasd                                 | сканирвование строки двойных слов (сравнивает AX с ES:DI) |
| lods источник                         | чтение из строки (копирует из DS:DI в AX)                 |
| lodsb                                 | чтение байта из строки (копирует из DS:SI в AL)           |
| lodsw                                 | чтение слова из строки (копирует из DS:SI в AX)           |
| lodsd                                 | чтение двойного слова из строки (копирует из DS:ESI в EAX)|
| stos приемник                         | запись в строку                                           |
| stosb                                 | запись байта в строку (копирует AL в ES:DI)               |
| stosw                                 | запись слова в строку (копирует AX в ES:DI)               |
| stosd                                 | запись двойного слова в строку (копирует AL в ES:EDI)     |
| ins источник, dx                      | чтение строки из порта                                    |
| insb                                  | чтение строки байт из порта (в ES:DI)                     |
| insw                                  | чтение строки слов из порта (в ES:DI)                     |
| insd                                  | чтение строки двойных слов из порта (в ES:EDI)            |
| outs dx, приемник                     | запись строки в порт                                      |
| outsb                                 | запись строки байт в порт (из DS:SI)                      |
| outsw                                 | запись строки слов в порт (из DS:SI)                      |
| outsd                                 | запись строки двойных слов в порт (из DS:ESI)             |
-----------------------------------------------------------------------------------------------------
| stc                                   | установить флаг переноса                                  |
| clc                                   | сбросить флаг переноса                                    |
| cmc                                   | инвертировать флаг переноса                               |
| std                                   | установить флаг направления                               |
| cld                                   | сбросить флаг направления                                 |
| lahf                                  | загрузить флаги состояния в AH                            |
| sahf                                  | загрузить флаги состояния из AH                           |
| pushf                                 | поместить FLAGS в стек                                    |
| pushfd                                | поместить EFLAGS в стек                                   |
| popf                                  | загрузить FLAGS из стека                                  |
| popfd                                 | загрузить EFLAGS из стека                                 |                               
| cli                                   | запретить прерывания                                      |
| sti                                   | разрешить прерывания                                      |
| salc                                  | установить AL в соответствии с CF                         |
-----------------------------------------------------------------------------------------------------
| lds премник, источник                 | загрузить адрес, используя DS                             |
| les премник, источник                 | загрузить адрес, используя ES                             |
| lfs премник, источник                 | загрузить адрес, используя FS                             |
| lgs премник, источник                 | загрузить адрес, используя GS                             |
| lss премник, источник                 | загрузить адрес, используя SS                             |
-----------------------------------------------------------------------------------------------------
| nop                                   | отсутствие операции                                       |
| lock                                  | префикс блокировки шины данных                            |
| ud2                                   | неопределенная операция                                   |
| cpuid                                 | идентификация процессора                                  |
-----------------------------------------------------------------------------------------------------
